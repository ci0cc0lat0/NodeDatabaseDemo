# To run the server we do node index.js in the terminal
- This will start the server listening on port 4000
- We can now type in the browser localhost:4000 to see the server
- Any changes to index.html will be reflected in the browser
